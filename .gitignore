# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Android (Capacitor native project)
android/app/build
android/build
android/.gradle
android/local.properties
android/captures
android/*.iml
android/.idea

# Signing secrets, never commit. Back the keystore up somewhere safe;
# losing it means future releases can't update installs of this app.
android/keystore/
android/keystore.properties

# iOS (Capacitor native project)
ios/App/Pods
ios/App/build
ios/App/DerivedData
ios/App/App.xcodeproj/xcuserdata
ios/App/App.xcodeproj/project.xcworkspace/xcuserdata
ios/App/App.xcodeproj/project.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
ios/App/CapApp-SPM/.swiftpm
ios/App/CapApp-SPM/.build
*.xcuserstate

# Local environment config. The Firebase values in here are publishable, but
# they still don't belong in the repo: they pin one project into the source,
# and deployed builds get them from the host's environment instead.
.env
.env.*
!.env.example
android/app/google-services.json

# Firebase admin credentials for the reminder worker. Full read/write access
# to Firestore and the ability to send push as the app - never commit.
# The broad *.json rule is deliberate: a key downloaded under whatever name
# the console gave it must not slip through. package.json is re-included
# after it, since a later negation is what overrides an earlier match.
worker/*.json
!worker/package.json
!worker/package-lock.json
worker/node_modules/
